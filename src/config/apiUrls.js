const BASE_URL = 'https://localhost:7200';

const API_URLS = {
  LOGIN: `${BASE_URL}/api/login`,
  REGISTER: `${BASE_URL}/api/register`,
  NOTES: `${BASE_URL}/api/note`,
  PROFILE: `${BASE_URL}/api/profile`
};

export default API_URLS;